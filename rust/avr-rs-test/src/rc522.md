| Константа                    | Код    | Простое объяснение                                                                  |
| ---------------------------- | ------ | ----------------------------------------------------------------------------------- |
| **`PCD_IDLE`**               | `0x00` | 💤 “Ничего не делай”. RC522 в режиме ожидания, не выполняет команд.                 |
| **`PCD_MEM`**                | `0x01` | 🧠 Перемещает 25 байт из внутренней памяти RC522 в буфер FIFO. (редко используется) |
| **`PCD_GENERATE_RANDOM_ID`** | `0x02` | 🎲 Генерирует случайный 10-битный ID (можно использовать для антиколлизии).         |
| **`PCD_TRANSCIEVE`**         | `0x0C` | 📡 Самая важная команда: **отправить байты и принять ответ** от карты.              |
| **`PCD_SOFTRESET`**          | `0x0F` | 🔁 Программный сброс чипа RC522 (аналог выключить/включить питание).                |
| **`PCD_CALCCRC`**            | `0x03` | 🔐 Посчитать CRC16 для передаваемых данных (для PICC SELECT и других команд).       |



| Регистр                                                      | Код           | Что хранит                                             | Простой смысл                                                                        |
| ------------------------------------------------------------ | ------------- | ------------------------------------------------------ | ------------------------------------------------------------------------------------ |
| **`COMMAND_REG`**                                            | `0x01`        | Текущая выполняемая команда                            | Сюда пишешь `PCD_TRANSCIEVE` и RC522 начинает передачу                               |
| **`FIFO_DATA_REG`**                                          | `0x09`        | Буфер для передачи/приёма байтов                       | 📥 Ты пишешь сюда данные, RC522 потом их отправляет; читаешь — получаешь ответ карты |
| **`FIFO_LEVEL_REG`**                                         | `0x0A`        | Сколько байтов сейчас в FIFO                           | Нужно, чтобы знать, сколько прочитать                                                |
| **`ERROR_REG`**                                              | `0x06`        | Флаги ошибок                                           | Показывает, случилась ли коллизия, ошибка CRC, переполнение и т.д.                   |
| **`COMM_IRQ_REG`**                                           | `0x04`        | Флаги завершения операций                              | Говорит, что передача закончена, ошибка или таймаут                                  |
| **`DIV_IRQ_REG`**                                            | `0x05`        | Флаги вспомогательных событий (например, CRC посчитан) | Используется при расчёте CRC                                                         |
| **`MODE_REG`**                                               | `0x11`        | Общие настройки работы RC522                           | В том числе, какие CRC значения использовать по умолчанию                            |
| **`TX_MODE_REG`**                                            | `0x12`        | Настройки передачи                                     | Как RC522 будет модулировать сигнал                                                  |
| **`RX_MODE_REG`**                                            | `0x13`        | Настройки приёма                                       | Как RC522 будет принимать сигнал от карты                                            |
| **`TX_CONTROL_REG`**                                         | `0x14`        | Управление антенной                                    | Чтобы включить/выключить антенну (`0x83` = включено)                                 |
| **`RFCFG_REG`**                                              | `0x26`        | Усиление антенны (Receiver Gain)                       | Чем выше — тем дальше “видит” карту                                                  |
| **`TMODE_REG`**, **`TPRESCALER_REG`**, **`TRELOAD_REG_L/H`** | `0x2A–0x2D`   | Таймер для протоколов                                  | Настраивают внутренние задержки обмена                                               |
| **`CRC_RESULT_REG_L/M`**                                     | `0x22 / 0x21` | Результат CRC                                          | После `PCD_CALCCRC` читаешь отсюда CRC                                               |
| **`VERSION_REG`**                                            | `0x37`        | Версия чипа                                            | Можно проверить, что модуль жив (`0x92` или `0x91` = OK)                             |


| Команда                 | Код    | Что делает                                                               | Пример                                 |
| ----------------------- | ------ | ------------------------------------------------------------------------ | -------------------------------------- |
| **`PICC_REQA`**         | `0x26` | 🔎 "Есть ли кто-то рядом?" — запрос наличия карты.                       | Карта отвечает “ATQA”.                 |
| **`PICC_ANTICOLL_CL1`** | `0x20` | 🧩 "Антиколлизия" — запросить UID карты, чтобы отличить от других.       | Возвращает UID (4 байта + BCC).        |
| **`PICC_SEL_CL1`**      | `0x93` | 📍 Указывает “уровень” карты (CL1 — первый, короткий UID).               | Используется перед SELECT.             |
| **`PICC_SEL_CMD`**      | `0x70` | 🎯 "Выбор карты" — подтвердить, что будем работать именно с этой картой. | Карта отвечает байтом SAK (тип карты). |



| Шаг | Команда                                               | Что делает                                   |
| --- | ----------------------------------------------------- | -------------------------------------------- |
| 1️⃣ | `PCD_SOFTRESET`                                       | Сброс RC522                                  |
| 2️⃣ | Настройка регистров                                   | Настроить таймер, усиление, включить антенну |
| 3️⃣ | `PICC_REQA`                                           | Найти карту                                  |
| 4️⃣ | `PICC_ANTICOLL_CL1`                                   | Получить UID                                 |
| 5️⃣ | `PICC_SEL_CL1` + `PICC_SEL_CMD`                       | Выбрать карту и получить SAK                 |
| 6️⃣ | Далее можно аутентифицировать, читать, писать, и т.д. |                                              |


┌────────────────────────┐
│  MCU (Arduino / Rust)  │
└──────────┬─────────────┘
           │   SPI (SCK, MOSI, MISO, CS)
           ▼
┌────────────────────────┐
│      RC522 (Reader)    │
│  ─ управляется SPI ─   │
│  ─ управляет антенной ─│
└──────────┬─────────────┘
           │  Радиосигнал (13.56 MHz)
           ▼
┌────────────────────────┐
│   RFID карта (MIFARE)  │
└────────────────────────┘



| Действие                    | Команда / Регистр                              | Что происходит                           |
| --------------------------- | ---------------------------------------------- | ---------------------------------------- |
| Сброс чипа                  | `PCD_SOFTRESET`                                | RC522 перезапускается                    |
| Настройка таймера           | `TMODE_REG`, `TPRESCALER_REG`, `TRELOAD_REG_*` | Контроль задержек обмена                 |
| Настройка передачи и приёма | `TX_MODE_REG`, `RX_MODE_REG`, `MODE_REG`       | Устанавливается режим ISO14443A          |
| Включение антенны           | `TX_CONTROL_REG = 0x83`                        | Антенна начинает излучать поле 13.56 MHz |
| Проверка связи              | `VERSION_REG`                                  | Если 0x91 / 0x92 — модуль жив            |


| Этап                   | Команда            | Описание                       |
| ---------------------- | ------------------ | ------------------------------ |
| MCU записывает в FIFO  | `PICC_REQA (0x26)` | Старт поиска карты             |
| MCU запускает передачу | `PCD_TRANSCIEVE`   | RC522 передаёт команду в эфир  |
| RC522 получает ответ   | `ATQA (2 байта)`   | Карта отвечает: “Да, я здесь!” |



| 🧩 Функция                                    | ⚙️ Что делает                 | 📋 Подробное описание                                                                                                           |
| --------------------------------------------- | ----------------------------- | ------------------------------------------------------------------------------------------------------------------------------- |
| `new(spi, cs)`                                | Конструктор                   | Создаёт объект RC522, запоминает SPI и пин CS.                                                                                  |
| `get_version()`                               | Читает версию чипа            | Считывает регистр `VERSION_REG` и возвращает номер версии MFRC522 (обычно 0x91 или 0x92).                                       |
| `rc522_read_reg(reg)`                         | Читает байт из регистра       | Через SPI шлёт адрес (с битом чтения) и получает значение из указанного регистра RC522.                                         |
| `rc522_write_reg(reg, val)`                   | Записывает байт в регистр     | Через SPI шлёт адрес (с битом записи) и записывает значение в нужный регистр.                                                   |
| `rc522_init()`                                | Инициализация модуля          | Делает сброс, настраивает таймеры, CRC, усиление антенны и включает RF-передатчик (готовит RC522 к работе).                     |
| `rc522_soft_reset()`                          | Сбрасывает чип                | Отправляет команду `PCD_SOFTRESET` и ждёт 50 мс.                                                                                |
| `picc_reqa(atqa)`                             | Проверяет наличие карты       | Отправляет команду `REQA (0x26)` — карта отвечает 2 байтами (ATQA). Если ответ есть — карта в поле.                             |
| `rc522_transceive(send, rx_buf, bit_framing)` | Универсальная передача        | Основная функция обмена: пишет данные в FIFO, запускает передачу (`PCD_TRANSCEIVE`), ждёт окончания и читает ответ.             |
| `rc522_set_bit_mask(reg, mask)`               | Устанавливает биты в регистре | Считывает значение регистра и ставит указанные биты в 1.                                                                        |
| `rc522_clear_bit_mask(reg, mask)`             | Сбрасывает биты в регистре    | Считывает значение регистра и сбрасывает (ставит в 0) указанные биты.                                                           |
| `picc_anticoll()`                             | Антиколлизия (чтение UID)     | Посылает команду `0x93 0x20`, чтобы карта ответила своим UID. Проверяет контрольную сумму (XOR) и возвращает [UID0..UID3, BCC]. |
| `picc_select(uid_cl1)`                        | Выбор карты                   | Отправляет `SELECT (0x93 0x70)` с UID и CRC, получает ответ (SAK – тип карты).                                                  |
| `rc522_calc_crc(data)`                        | Вычисляет CRC                 | Передаёт массив в FIFO, запускает команду `PCD_CALCCRC`, ждёт окончания, считывает CRC из регистров.                            |
| `rc_read(atqa, serial)`                       | Главная функция чтения карты  | Делает REQA → ANTICOLL → SELECT, печатает ATQA, UID, SAK, возвращает UID как строку.                                            |
